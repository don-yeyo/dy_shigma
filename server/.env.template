# Configuración del Servidor Express
PORT=5000
NODE_ENV=development

# Database Configuration (MySQL / AWS RDS)
DB_HOST=dydb2-instance-1.cz8kik28igwg.us-east-1.rds.amazonaws.com
DB_USER=DBAdmin_Acceso_A_Planta
DB_PASS=tu_contraseña_aqui
DB_NAME=Shigma
DB_PORT=3306
DB_TIMEZONE=-03:00

# Feature Flags
ENABLE_GOOGLE_LOGIN=true

# Push Notifications (VAPID Keys)
VAPID_PUBLIC_KEY=BMCoIK30rce0vb76sFsTGq7m4B5lh9Ns4PjRz0tijUuerVgAch1bEfarbEWELJ44nCrXad-Aq0JPmnhSTUDwh-E
VAPID_PRIVATE_KEY=ZwsvXyut9E9D-LzLWaqVTHg8iCO8lVEUOo9Q6yDlAyA

# Autenticación (Azure AD) - Opcional para producción
AZURE_AD_CLIENT_ID=
AZURE_AD_TENANT_ID=
# Emails de sysadmin de bootstrap — fallback cuando la tabla `usuarios` esté vacía
# Separados por comas. Ej: admin@empresa.com,otro@empresa.com
ADMIN_EMAILS=tu_email@empresa.com

# Límites de edición para el rol Registrador
# N días hacia atrás que puede editar un registro
REGISTRADOR_MAX_EDIT_DAYS=7
# Cuántas veces puede editar un mismo registro
REGISTRADOR_MAX_EDITS_PER_RECORD=3
# Permitir que el rol Registrador elimine registros bajo las mismas reglas de antigüedad/edición (true/false)
REGISTRADOR_PUEDE_ELIMINAR=true

# Cantidad de registros por página en el historial (paginación del lado del servidor)
HISTORIAL_PAGE_SIZE=20


# Configuración de APIs externas (Históricas / Opcionales)
# Si se requiere integrar con Finnegans ERP en el futuro
FINNEGANS_CLIENT_ID=
FINNEGANS_CLIENT_SECRET=
FINNEGANS_TOKEN_URL=https://api.teamplace.finneg.com/api/oauth/token
FINNEGANS_API_BASE=https://api.finneg.com/api
FINNEGANS_EMPRESA_COD=1
FINNEGANS_TIMEOUT=30
